<h2>Sign Up</h2>
<P>YMN will never share your password with third-parties and will only email you if you forget your password and want to reset it.</p>

{{#if signUpSuccess}}
  <p>
    Your account was created successfully! {{#link-to 'index'}}Log in{{/link-to}} to start using You Might Need!
  </p>
{{else}}
  {{#if showErrorMsg}}
    <p class="error">
      Sorry, that email is already registered. Do you need to {{#link-to 'reset'}}reset your password{{/link-to}}?
    </p>
  {{/if}}
  <form id="signupForm" method="post" {{action "signUp" on="submit"}}>
    <div class="">
      <label for="loginEmail">Email</label><br>
      {{input
        id="loginEmail"
        placeholder="your@email.com"
        required="true"
        type="email"
        value=email
      }}
    </div>

    <div class="">
      <label for="loginPassword">Password</label><br>
      {{input
        id="loginPassword"
        required="true"
        type="password"
        value=password
      }}
    </div>

    <button type="submit" class="btn btn-primary">Create Account</button>
  </form>

{{/if}}
